{
  "compilerOptions": {
    "outDir": "dist",
    "baseUrl": "src",
    "target": "es6",
    "module": "CommonJS",
    "strict": true,
    "noImplicitAny": true,
    "esModuleInterop": true,
    "jsx": "react-native",
    "emitDeclarationOnly": true,
    "declaration": true,
    "declarationMap": true,
    // what do you think of the options below?
    // "noEmit": true, // I noticed that running `npx tsc` will generate .js files so we want this?
    "skipLibCheck": true // also noticed that running `npx tsc` will highlight a bunch of errors from node_modules
  },
  "include": ["./src/*"],
  "exclude": ["./node_modules", "./src/DefaultTouchProcessor.ts"]
}
